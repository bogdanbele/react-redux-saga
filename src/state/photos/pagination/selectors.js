const getPage = (state) => state.page;
const getTotalPages = (state) => state.totalPages;
const getElementsPerPage = (state) => state.elementsPerPage;
const getTotalElements = (state) => state.totalElements;