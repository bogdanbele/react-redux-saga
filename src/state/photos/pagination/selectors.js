export const getPage = (state) => state.pagination.page;
export const getTotalPages = (state) => state.pagination.totalPages;
export const getElementsPerPage = (state) => state.pagination.elementsPerPage;
export const getTotalElements = (state) => state.pagination.totalElements;
export const getPagination = (state) => state.pagination;